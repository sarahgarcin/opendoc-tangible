(function() {
  var phantom;

  phantom = require('phantom');

  phantom.create(function(ph) {
    return ph.createPage(function(page) {
      return page.open("http://www.mdscollections.com/cat_mds_accessories.cfm", function(status) {
        var finishedFunc, someFunc;
        console.log("opened page? ", status);
        someFunc = function(aaa) {
          var attribute_to_want, h2Arr, results, x, _i, _ref;
          attribute_to_want = aaa;
          h2Arr = [];
          results = document.querySelectorAll(attribute_to_want);
          for (x = _i = 0, _ref = results.length; 0 <= _ref ? _i < _ref : _i > _ref; x = 0 <= _ref ? ++_i : --_i) {
            h2Arr.push(results[x].href);
          }
          return {
            h2: h2Arr,
            aaa: this.aaa,
            sample_dom: results[0]
          };
        };
        finishedFunc = function(result) {
          console.log(result);
          return ph.exit();
        };
        return page.evaluate(someFunc, finishedFunc, '.listing_product_name');
      });
    });
  });

}).call(this);
